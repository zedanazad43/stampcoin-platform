# Frontend ABIs

This folder stores ABI files used by the frontend for contract interactions.

Generate/refresh files using:

- `npm run web3:compile`
- `npm run web3:export-abis`

Expected outputs:

- `StampCoinToken.abi.json`
- `StampNFT.abi.json`
